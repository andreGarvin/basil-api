// import * as crypto from 'crypto';

// // models
// import userModel from './model';

// // utils
// import logger from "../../common/logger";

// // types
// import { UserAccount } from "./types";

// class AuthenticationController {
//   async authenticate(email: string, password: string): Promise<UserAccount> {}
//   async createAccount(email: string, password: string): Promise<void> {}
//   async verifyAccount(verificationToken: string): Promise<{ email: string }> {}
//   private async accountSetup(userId: string): Promise<void> {}
//   async updatePassword(
//     user: string,
//     oldPassword: string,
//     newPassword: string
//   ): Promise<void> {}
//   async resetPasswordWithToken(resetPasswordToken: string): Promise<void> {}
//   async reactivateAccount(resetPasswordToken: string): Promise<void> {}
//   async sendVerificationEmail(email: string): Promise<void> {}
//   async sendResetPasswordEmail(email: string): Promise<void> {}
// }

// export default new AuthenticationController();
